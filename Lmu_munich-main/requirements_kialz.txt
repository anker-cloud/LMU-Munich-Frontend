# ki-alz Pipeline — Requirements
# --------------------------------
# Install with:
#   pip install -r requirements_kialz.txt
#
# NOTE: Install torch separately first (choose your CUDA version):
#   CPU only:   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   CUDA 11.8:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   CUDA 12.1:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# ── Core deep learning ────────────────────────────────────────────────────────
pytorch-lightning
torchmetrics
torchio

# ── Explainability ────────────────────────────────────────────────────────────
# captum requires numpy<2.0 but numpy 2.x is already installed and works fine.
# Install captum separately with --no-deps to skip the numpy downgrade:
#   pip install captum --no-deps
medcam                          # Grad-CAM++ for 3D-CNN MRI branch
opencv-python-headless          # required by medcam (headless = no display needed on servers)

# ── Tabular model ─────────────────────────────────────────────────────────────
tabpfn                          # GPU required for .p checkpoint files

# ── API ───────────────────────────────────────────────────────────────────────
fastapi
uvicorn
python-multipart                # required for FastAPI file uploads
pydantic

# ── Data / numerics ───────────────────────────────────────────────────────────
numpy
pandas
scipy
scikit-learn
odfpy                           # reading .ods files

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib
seaborn                         # used in confusion matrix visualisation

# ── Utilities ─────────────────────────────────────────────────────────────────
nibabel                         # NIfTI file I/O
xgboost==2.0.3                  # pin to version with pre-built wheels (newer versions require CMake 3.18+)

# ── NOT included (reason) ─────────────────────────────────────────────────────
# aim      — experiment tracker, training only; fails to install on Python 3.13 / Windows
#            (aimrocks==0.5.* not available). Safe to skip for inference.
# torch    — install separately (see top of file) to match your CUDA version
