FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package.json package-lock.json* ./

# CLEANUP: Remove any existing node_modules from the build context
RUN rm -rf node_modules

# Force a clean install
RUN npm install

COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev"]